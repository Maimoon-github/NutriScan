# Core Django Framework
Django==5.0.6
djangorestframework==3.15.1

# Image Processing & OCR
Pillow==10.3.0
paddleocr==2.7.3              # Production OCR engine (Urdu/English support)
paddlepaddle==2.6.2           # PaddlePaddle deep learning framework (updated)

# LLM & AI Integration (Phase 2)
langchain>=0.3.19             # LLM orchestration framework (updated for compatibility)
langchain-community>=0.3.18   # Community integrations (matched version)
ollama==0.1.6                 # Ollama Python SDK for Qwen 2.5 7B

# Vector Database for RAG
pinecone-client==3.0.0        # Vector database for regulatory documents
# sentence-transformers==2.3.1  # Text embeddings for semantic search (DISABLED - causes PyTorch DLL issues on Windows)
# chromadb==0.4.22              # Alternative local vector DB (DISABLED - not currently used)

# Text Processing & NLP
spacy==3.7.2                  # Named Entity Recognition
ftfy==6.1.3                   # Text encoding fixes for Urdu

# For future phases (AWS/Google Cloud)
# boto3==1.34.84              # AWS SDK (Textract, S3)
# google-cloud-vision==3.7.2  # Google Cloud Vision API
# openai==1.23.2              # OpenAI GPT-4o-mini

# API & Utilities
python-dotenv==1.0.1          # Environment variable management
requests==2.31.0              # HTTP library
python-multipart==0.0.9       # File upload support

# Observability & Error Tracking
sentry-sdk[django]==2.18.0    # Error tracking and performance monitoring

# CORS Support for mobile apps
django-cors-headers==4.3.1

# Development Tools
pytest==8.1.1
pytest-django==4.8.0
